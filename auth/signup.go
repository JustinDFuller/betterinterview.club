package auth

/**
user, err := organization.NewUser(email.Address)
if err != nil {
	log.Printf("Error creating new user in /auth/login: %s", err)
	http.ServeFile(w, r, "./error/index.html")
	return
}

if _, err := organizations.AddUser(org, user); err != nil {
	log.Printf("Error adding new user in /auth/login: %s", err)
	http.ServeFile(w, r, "./error/index.html")
	return
}
*/
